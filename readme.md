Forked from [hyper-snazzy](https://github.com/sindresorhus/hyper-snazzy)

### installation

```
$ cd ~/.hyper_plugins/local
$ ln -s path/to/hyper-theme ./hyper-theme

# then add "hyper-theme" to local plugins
$ cd ~ && nano .hyper.js
```
